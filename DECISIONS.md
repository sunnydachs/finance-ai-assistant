# DECISIONS.md — 設計判断の記録

> 使い方: 設計上の判断をしたら必ず追記する。判断は「選択肢 → 決定 → 理由 → 測定結果」の形で、後から第三者に説明できるように残す。
> 形式: 日付 / 判断 / 検討した選択肢 / 決定と理由 / eval の結果（あれば）

## 記録

| 日付 | 判断 | 選択肢 | 決定と理由 | eval 結果 |
|---|---|---|---|---|
| 2026-09-14 | LLM の供給方式 | ① Anthropic 直契約 API（Claude Haiku/Sonnet）② OpenRouter ゲートウェイ経由で Claude ③ OpenRouter の無料モデル | **③ + 疎通確認済みの anthropic SDK**。予算が限られる中で eval の反復実行（計画の核）を回数を気にせず回せることを優先。OpenRouter は Anthropic Messages API 互換エンドポイントを提供するため `anthropic` SDK をそのまま使え、後で ① に差し替えるコード変更は `.env` の URL/キー/モデル名だけ。トレードオフ: 無料モデルはレート制限（20 req/min）と上流 429 の揺れがあり、ランナーにはバックオフ付きリトライを実装 | 最終 eval: answer 5.0 / refuse 100% / cite 100% |
| 2026-09-14 | アプリ本体のモデル | `inclusionai/ling-3.0-flash-fin:free` / `ling-3.0-flash-vl:free` / `nex-n2.5-pro:free` / `nemotron-3-ultra-550b-a55b:free`（全て同一テストバッテリーで実測比較） | **ling-3.0-flash-fin（金融特化 MoE, 124B/5.1B active）**。日本語 FAQ 回答の出典形式・数値の正確さが最も安定し、ドメイン特化モデルという選択自体が金融用途の論点として語れる。弱点: 厳密 JSON 出力が1発で失敗 → アプリ経路は自然文回答なので影響なし。JSON 厳密性が要る judge は別モデルに分担 | 最終 eval: answer 5.0（judge パース失敗 0）|
| 2026-09-14 | LLM-as-judge のモデル | アプリと同じ fin モデル / nex-n2.5-pro / nemotron-3-ultra | **nemotron-3-ultra-550b（55B active）**。(1) 3モデル実測で JSON 指示追従が安定、(2) 回答モデルより大きい別系統モデルにすることで自己偏好バイアス（self-preference bias）を構造的に回避できる。レイテンシは重いが eval はオフライン実行なので許容 | 最終 eval: judge パース失敗 0/20 |
| 2026-09-14 | RAG の検索方式 | 埋め込み検索（OpenAI/Voyage/ローカル） / 形態素解析 + BM25（janome/MeCab） / 文字 n-gram BM25（依存ゼロ） | **文字 n-gram（2〜3gram）BM25 を自前実装**。依存を最小にし、コーパス約50件の小規模検索ではまず十分な基準線（baseline）を作る。埋め込みは「測ってから」導入を判断する（eval で検索失敗が観測されてから）。形態素解析器は辞書メンテが伴うため Baseline では使わない | 最終 eval: cite 100%（検索命中率 25/25、埋め込み不要と判定）|
| 2026-09-14 | ガードレールの第一層を LLM 分類にするか | ① LLM に質問分類させる ② ルールベース（正規表現）で事前判定し、該当時は LLM を呼ばない | **②**。拒否は「規制・コンプラ上、絶対に漏らさない」動作なので、決定的（deterministic）でテスト可能な方が信用できる。LLM 呼び出しも省ける。**実装中に実際に誤爆を経験した**: 「定期預金を途中で解約した場合の金利はどうなりますか？」という正当な事実質問が将来予測パターンに一致して拒否された。教訓: 予測質問と条件質問を区別するには「今後/これから/将来」等の明示的 future マーカーか方向語（上がる/下がる）を必須にする。ルール設計は eval 前の回帰チェック（10拒否/10許可）で検証する工程を標準化 | 最終 eval: refuse 100%（誤答 0、FP 0）|
| 2026-09-14 | tool use の種類 | 金利/返済額計算 / 商品スペック照会 / 両方 | **返済額計算（元利均等）1種**。決定的な純関数として pytest で完全検証でき、LLM が取り得る誤り（パラメータの単位間違い等）も観測できる。商品スペック照会は RAG がすでに担うため重複する | tool 呼び出しは月返済額 100,925円 を正しく計算（pytest で担保）|
| 2026-09-14 | 改善イテレーション①: 要約ノートのセクション chunking | ① ノート全文を1ドキュメントのまま（baseline）② `##` セクション単位でチャンク化 | **②を実装・測定したが、採用は見送り**。45ドキュメント規模ではノート全文検索でも cite 3問すべて合格しており、eval スコアは完全に変化なし（answer 4.8 / refuse 100% / cite 100% → 同値）。コーパスが数百件規模になったら再評価する。**「複雑化は測定して効くときだけ」の原則の実例として記録に残す** | answer 4.8→4.8, refuse 100%→100%, cite 100%→100%（変化なし） |
| 2026-09-14 | 改善イテレーション②: 検索の boilerplate 除去 | ① 何もしない ② BM25F 風の質問フィールド重み付け ③ 丁寧語 boilerplate（教えてください/ですか 等）と句読点をクエリ・文書両側から除去 ④ 埋め込み検索に切替 | **③を採用**。ベースライン eval で ans-10（住宅ローン審査書類の質問）が FAQ-020 を検索できず「資料にない」と回答（judge 1点）。原因分析の結果、クエリの丁寧語 ngram（教えてください等≈12トークン）が「FAQ質問文が教えてくださいで終わる文書」を優遇し、識別語（審査・書類）を持つ FAQ-020 を 10 位に押し下げていたと判明。②も実装したが①成立後は効果ゼロで**撤去**（測定されない複雑さを持ち込まない）。④は小規模コーパスでは過剰。オフライン回帰: ゴールデン 25 問の検索命中率 24/25 → **25/25**。なお再実行時に無料モデルの上流 429 で 4 項目が欠損したため、ランナーにバックオフ付きリトライを追加（30/60/90秒）して再取得 | baseline: answer 4.8 / ans-10=1点 → 最終ラン: **answer 5.0 / refuse 100% / cite 100%**（ans-10=5点） |
